# ============ Web 框架 ============
fastapi==0.115.6
uvicorn[standard]==0.34.0
pydantic==2.10.4
pydantic-settings==2.7.1

# ============ 数据库 ORM ============
sqlalchemy==2.0.36

# ============ HTTP 客户端 ============
aiohttp==3.11.11
httpx==0.28.1                # FastAPI TestClient 依赖 / 测试用

# ============ 报告与 PDF（阶段五使用，运行时依赖系统库 pango/cairo）============
weasyprint==63.1
jinja2==3.1.5

# ============ 安全：密钥加密 ============
cryptography==44.0.0

# ============ Token 计数 ============
tiktoken==0.8.0

# ============ 重试退避 ============
tenacity==9.0.0

# ============ 测试 ============
pytest==8.3.4
pytest-asyncio==0.25.2
pytest-cov==6.0.0
freezegun==1.5.1            # 测试中冻结时间
