[tool.pytest.ini_options]
# 让测试可直接 import app.*（backend 目录入 sys.path）
pythonpath = ["."]
# pytest-asyncio 自动识别 async 测试，无需逐个标注 @pytest.mark.asyncio
asyncio_mode = "auto"
asyncio_default_fixture_loop_scope = "function"
testpaths = ["tests"]
python_files = ["test_*.py"]
python_functions = ["test_*"]
